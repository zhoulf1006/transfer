node_modules/
out/
dist/
release/
*.log
.DS_Store
*.tsbuildinfo
# 本地签名/公证凭据(Apple ID、专用密码等),绝不提交
.env.local
.env.*.local
# provisioning profile:含团队标识与设备清单,按开发者/机器各不相同,绝不提交。
# 获取方式见 electron-builder.yml 的 mas/masDev 段。
build/*.provisionprofile
# Playwright 失败产物(截图/trace/error-context),每次跑覆盖,不入库
test-results/
playwright-report/
# 施工文档(tickets、施工过程记录):本机保留跨轮复用,但不入库(ADR-0019)。
# 它腐烂最快且不得作为现状依据;要跨机器留存的内容须升级到 specs/features/adr/CONTEXT。
docs/.workings/

# 国内镜像的部署目标(服务器地址不进公开仓库),见 build/deploy-site-cn.sh
.deploy.env
# 原型画廊的第三方渲染库(3.4MB,可再生;缺失时画廊降级显示图源文本)
# 还原:curl -sL -o docs/prototypes/vendor/mermaid.min.js https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js
docs/prototypes/vendor/
# JetBrains IDE 本地配置
.idea/
