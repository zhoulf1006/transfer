# 领域文档位置(agent 配置)

单仓库结构(非 monorepo):

- **领域词汇表**:根目录 [`CONTEXT.md`](../../CONTEXT.md) —— 术语、禁用同义词、已消解歧义。grilling/domain-modeling 会话中随手更新它。
- **ADR**:[`docs/adr/`](../adr/) —— 一决策一文件,只追加;索引见其 README。
- **系统设计综述**:[`docs/DESIGN.md`](../DESIGN.md) —— 现状描述(历史存档性质,决策以 ADR 为准)。
- **复盘**:`docs/*.md` 中的踩坑叙事(如 electron-graceful-quit)—— 持久保留。
