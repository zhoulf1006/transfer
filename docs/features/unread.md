# 新消息提醒

## 概述

窗口不在前台时收到消息毫无感知,容易漏看。系统级与应用内双层提醒:桌面层面有角标/闪烁,回到应用一眼看到谁发了几条。

## 能力

- macOS:Dock 图标显示未读数字角标;Windows:任务栏图标闪烁
- 应用内:设备行显示该会话未读数角标
- 进入会话即清零该会话未读;窗口获得焦点后系统级提醒停止
- 仅对新消息计数,消息状态变化(如传输完成)不打扰

## 边界与不做

- macOS 的 Dock 数字角标需系统通知权限
- 不做声音提醒、不做系统通知横幅
